## 1. 重复码 FEC 砌块(iaq-fec)

- [x] 1.1 新增 `src/vit_iaq_semcom/fec.py`:重复码编码(速率 1/r)+ 多数表决解码
- [x] 1.2 实现给定 BER 的解码误码率闭式估计(二项尾概率)
- [x] 1.3 单测:无噪声往返逐位相等、比特膨胀=L·r、纠正少数错误、估计随 r 单调下降、
      估计与 BSC 蒙特卡洛在容差内一致

## 2. 头号风险验证实验(闸门,先做)

- [x] 2.1 写脚本:对一批图分别用「原图」与「1 bit 基础层重建图」算注意力 a_i
- [x] 2.2 度量两者派生 M_i 分配的一致性(排序相关/重叠度)与最终准确率差,输出 JSON
- [x] 2.3 纯 CPU 小样本先量「同权重同输入跑两遍 attention_scores 的浮点漂移」,确认
      注意力量化档位(16 档起点)是否足够锁一致
      → 结论(CPU n=8):max_abs_drift=0.0、bit_identical=True、quant16_consistent=True。
      同硬件同输入漂移为零,仿真链路两端派生天然一致;16 档量化为零成本安全边际、
      为跨硬件鲁棒性背书。设计假设成立,A1 仿真可零训练直接派生。
- [ ] 2.4 与用户约定时长/样本后跑一次(GPU,先问);据结果判定走零训练还是退路
      (b588 换工作点 / 小修复头 / 蒸馏),记录结论

## 3. A1 信源链路(iaq-a1-metadata-free)

- [x] 3.1 新增 A1 编解码模块(`src/vit_iaq_semcom/a1.py`):基础层 1 bit 均匀量化 +
      增强层切分,两层比特预算守恒(196+196)
- [x] 3.2 实现注意力量化(16 档)并接入增量分配算法(复用 `bit_allocation.py`,
      增强层预算 196、输入改量化 a_i)
- [x] 3.3 实现两端确定性派生:收端解基础层→算 a_i→派生 ΔM_i;增强层按派生 ΔM_i 自动切分
      (利用均匀量化嵌入式性质:T bit 索引高 M_base 位=基础层)
- [x] 3.4 接入基础层重复码 FEC(用 §1 砌块),开销计入总带宽
- [x] 3.5 单测(8/8 通过):相同 a_i 两端 ΔM_i 一致 + 量化吸收漂移;不传 M_i 图/段长度;
      增强层边界自动对齐无失步;增强层全毁时退化到基础层质量、逐 patch 误差有界无崩盘;
      基础层 FEC 纠少数错往返一致

## 4. 管线对照分支(iaq-pipeline)

- [x] 4.1 `IAQPipeline` 增加 A1 模式分支(`_run_a1`:两端基础层重建上各自算注意力派生,
      收端解基础层后重新算 a_i,诚实建模基础层受损)
- [x] 4.2 增加 EEP 模式(元信息&载荷等强重复码)与 UEP 模式(元信息强、载荷弱),
      `channel.awgn_channel_fec`
- [x] 4.3 单测(mock encoder,4/4 通过):A1 端到端跑通+无噪重建保真;A1 极低 SNR 有底无崩;
      EEP/UEP 无噪与 IAQ 理想一致;iaq 默认模式回归保护

## 5. 六线对照实验

- [x] 5.1 写六线实验脚本 `run_a1_sixline_experiment.py`(① 均匀 ② 理想 ③ 裸传 ④ EEP
      ⑤ UEP ⑥ A1),记录每线标称信道比特(同带宽口径),固定 ρ=0.25 扫 SNR,结果写 JSON
- [ ] 5.2 图1 匹配场景:每个 SNR 按当前 SNR 设计 FEC → 验证 ④⑤⑥ 高 SNR 收敛到 ②
      (脚本已支持:不给 --design-snr;**待模型场次跑**)
- [ ] 5.3 图2 失配场景(杀招):按固定点(8dB)设计 FEC 再扫真实 SNR → 验证 ⑤ 再悬崖、
      ⑥ A1 有底平滑(脚本已支持:--design-snr 8;**待模型场次跑**)
- [ ] 5.4 与用户约定时长/样本后跑高保真版(GPU,先问);加 --replot 纯绘图走 CPU
- [x] 5.5 绘图脚本:六线曲线(横轴标注 ρ、场景标题)+ JSON 内 channel_bits 开销记账;
      --replot 纯 CPU 出图已验证

## 6. 收尾

- [ ] 6.1 自审 + `openspec validate`;同步 delta specs 到主 specs 并归档(先同步再归档)
- [x] 6.2 更新 README Step 6c 路线图(A1 元信息-free + 六线对照)
- [x] 6.3 更新记忆 [[vit-iaq-planb-design]] 进度
